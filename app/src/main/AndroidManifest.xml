<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="Echo Sync Extension"
        android:allowBackup="false">

        <meta-data
            android:name="extensionClass"
            android:value="com.yourorg.echosync.MyExtension" />

        <meta-data
            android:name="extensionName"
            android:value="Echo Sync" />

        <meta-data
            android:name="extensionAuthor"
            android:value="YourName" />

        <meta-data
            android:name="extensionVersion"
            android:value="1" />

        <meta-data
            android:name="extensionDescription"
            android:value="Local WiFi Sync Extension" />

    </application>

</manifest>